# EXACT ION Workstation Implementation Plan
> For agentic workers: use superpowers:subagent-driven-development for bounded asset tasks. The Site owner alone implements and integrates the application.

**Goal:** Build the approved Chinese Three.js disassembly workstation from official Bosch CAD and evidence-aware guidance.
**Architecture:** Controlled React exploration state drives independent Three.js CAD meshes. Local catalogue and instructional assets preserve source provenance.
**Tech Stack:** React, TypeScript, Three.js, Vinext, Base UI/Shadcn, Node tests, private Sites.

### Task1 — First preview
- [x] Scaffold site with required components; register one private Site.
- [x] Save approved design and read source evidence.
- [x] Replace app/page.tsx, app/layout.tsx, app/globals.css and components/workbench.tsx with the three-column working surface and official drawing.
- [x] Run npm run dev; request printed local URL once successfully and open in Codex.

### Task2 — Assets
- [x] Worker converts the official LPack STEP to GLB with unique mesh ids, source colors and bounds manifest outside Site.
- [x] Content worker deduplicates36 catalogue entries and creates9 source-labelled Chinese stages outside Site.
- [x] Owner validates and integrates into public/models, public/reference and lib/data; preserve CAD/service-number distinctions.

### Task3 — Scene
- [x] Add tests/exploration.test.mjs for zero/full/reversed transforms, clamped stage/amount, selection while isolated and reset. Run node --test tests/*.test.mjs to see expected failing assertions.
- [x] Implement lib/exploration.mjs with initialState, transition(state,action), offsetForPart(part,amount); add typed declaration. Rerun tests until pass.
- [x] Implement components/tool-viewer.tsx with GLTFLoader, OrbitControls, lighting, raycast selection, reversible eased translation, isolation, responsive sizing, disposal and errors.

### Task4 — Workstation
- [x] Wire controlled Tabs, Slider, Progress and Dialog primitives into stages, playback, catalogue, source views and model-part list.
- [x] Ensure every visible action works and every CAD instance can be selected independently. Mark unresolved source mappings.
- [x] Add compact WebMCP state/selection/stage/explosion tools after reading the reference.
- [x] Complete responsive layout, keyboard alternatives and reduced-motion behavior.

### Task5 — Verify and publish
- [ ] Run state tests, model asset validation, npx tsc --noEmit and npm run build; fix proven causes of any failures.
- [x] Obtain independent read-only spec and code-quality review; resolve findings.
- [ ] Commit validated Site-only source, push with ephemeral credential, package/save and deploy owner-only.
- [ ] Verify successful deployment and update the existing preview tab; hand off deployed URL with scope limitation.
